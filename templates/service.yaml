apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-project.fullname" . }}
  labels:
    {{- include "sample-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-project.selectorLabels" . | nindent 4 }}
