apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-project.fullname" . }}-test-connection"
  labels:
    {{- include "sample-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
